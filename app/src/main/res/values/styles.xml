<resources>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/icons</item>
    </style>

    <style name="AppTheme" parent="MaterialDrawerTheme.Light">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="SensorButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/accent</item>
        <item name="colorButtonNormal">@color/primary</item>
    </style>

    <style name="SensorCard">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="SensorTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
        <item name="android:textSize">16sp</item>
        <item name="android:color">@color/primary_text</item>
    </style>

    <style name="SensorSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/accent</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/secondary_text</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/secondary_text</item>
    </style>

    <style name="IntroEditText" parent="AppTheme">
        <item name="android:textColorHint">@color/md_white_1000</item>
        <item name="android:textSize">16sp</item>
        <item name="colorControlNormal">@color/md_white_1000</item>
        <item name="colorControlActivated">@color/md_white_1000</item>
    </style>

    <style name="IntroTextLabel" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/md_white_1000</item>
        <item name="colorControlNormal">@color/md_white_1000</item>
        <item name="colorControlActivated">@color/md_white_1000</item>
    </style>

    <style name="IntroTextErrorLabel" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/md_white_1000</item>
    </style>

    <style name="DeviceCard">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:layout_margin">8dp</item>
        <item name="cardCornerRadius">2dp</item>
    </style>

    <style name="DeviceCardInner">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">140dp</item>
        <item name="android:padding">4dp</item>
    </style>

    <style name="DeviceCardTitle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:textSize">14sp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">2</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
    </style>

    <style name="DeviceCardBottom">
        <item name="android:orientation">horizontal</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:gravity">bottom|center_horizontal</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <style name="BackgroundImageView">
        <item name="android:alpha">1.0</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:src">@drawable/background_common</item>
    </style>

    <style name="HeaderBackgroundImageView">
        <item name="android:alpha">1.0</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:src">@drawable/header_background_common</item>
    </style>

</resources>
